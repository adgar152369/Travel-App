<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Places</title>
</head>

<body>
    <h1>Places</h1>

    <ul>
        <% for (let place of userPlaces) { %>
            <li>
                <a href="/places/<%= place.id %>" style="color: black"><%= place.country %></a>
                <span style="color: black"> - <%= place.city %></span>
            </li>
        <% } %>
    </ul>

    <a href="/">Home</a>
</body>

</html>